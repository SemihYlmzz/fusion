import 'package:flutter/material.dart';

class BottomShared extends StatelessWidget {
  const BottomShared({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
