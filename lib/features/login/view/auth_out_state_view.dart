import 'package:flutter/material.dart';

class AuthOutStateView extends StatelessWidget {
  const AuthOutStateView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
